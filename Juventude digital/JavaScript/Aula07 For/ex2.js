//Informar quantas vezes vai se repetir a palavra banana
const prompt = require("prompt-sync")()

const max = Number(prompt("Digite o max: "))

for(let i = 0; i <= max; i++){
    console.log("Banana")
}